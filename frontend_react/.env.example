# Base URL for backend API (optional). If not provided, the app runs in stub mode.
REACT_APP_API_BASE=https://your-backend.example.com
# Alternative variable name supported:
REACT_APP_BACKEND_URL=
# Optional UI/env toggles:
REACT_APP_PORT=3000
REACT_APP_LOG_LEVEL=info
