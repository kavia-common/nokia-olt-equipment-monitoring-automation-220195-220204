# backend_node environment example
#
# Copy this file to ".env" in the backend_node directory and adjust values as needed.
# NOTE: Do NOT commit your real .env to source control.

# Core server configuration
PORT=4000
NODE_ENV=development

# CORS: frontend origin (React dev server by default)
FRONTEND_ORIGIN=http://localhost:3000

# Nokia 7360 OLT connection defaults
# You can override host/username/password on each /connect request.
OLT_HOST_DEFAULT=202.39.123.124
OLT_SSH_PORT=22
OLT_USERNAME=your-olt-username
OLT_PASSWORD=your-olt-password

# API authentication
# Set a strong random token and include it as
#   Authorization: Bearer <API_AUTH_TOKEN>
# on every request (except /health and /openapi.json which are public).
API_AUTH_TOKEN=change-me-to-a-long-random-string

# Logging configuration
# One of: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Request logging: set to "false" to disable HTTP access logs
REQUEST_LOGGING=true

# CORS credentials
# Enable only if you need cookies/credentials between frontend and backend.
ALLOW_REQUEST_CREDENTIALS=false
